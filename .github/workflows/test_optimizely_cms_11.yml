name: optimizely cms 11

on:
  push:
    branches: [ main ]
    paths:
      - 'src/net48/**.*'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/net48/**.*'

defaults:
  run:
    working-directory: ./src/net48

jobs:
  test:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v1

    - name: Setup VSTest Path
      uses: darenm/Setup-VSTest@v1

    - name: restore dependencies
      run: nuget restore

    - name: build
      run: msbuild

    - name: test
      run: vstest.console.exe Lorem.Testing.Optimizely.CMS.Test\bin\Debug\net48\Lorem.Testing.Optimizely.CMS.Test.dll /TestAdapterPath:Lorem.Testing.Optimizely.CMS.Test\bin\Debug\net48\

    - name: test-min
      run: vstest.console.exe Lorem.Testing.Optimizely.CMS.Test.Min\bin\Debug\net471\Lorem.Testing.Optimizely.CMS.Test.Min.dll /TestAdapterPath:Lorem.Testing.Optimizely.CMS.Test.Min\bin\Debug\net471\